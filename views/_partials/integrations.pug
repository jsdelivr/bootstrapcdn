hr
br
a(href=item.url, target="_blank")
    img.img-responsive.thumbnail.center-block(src=helpers.buster(item.img), alt=item.name)
h3
    a(href=item.url, target="_blank")=item.name
| Platform: 
span=item.plat
br

//- vim: ft=pug sw=4 sts=4 et:
