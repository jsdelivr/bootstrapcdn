extends layout

block content
    h2 Integrations
    .row
        .col-md-6.text-center
            each item in col1
                include _partials/integrations.pug

        .col-md-6.text-center
            each item in col2
                include _partials/integrations.pug
    .row
        .col-md-12.text-center
            p
                a.btn.btn-success(href='mailto:justindorfman@gmail.com?subject=' + encodeURIComponent('BootstrapCDN Integration Submission')) Submit Yours

//- vim: ft=pug sw=4 sts=4 et:
